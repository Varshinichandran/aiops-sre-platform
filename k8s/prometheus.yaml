global:
  scrape_interval: 15s  # how often to scrape targets

scrape_configs:
  - job_name: "fastapi-app"
    static_configs:
      - targets: ["127.0.0.1:8000"]  # Use localhost for local Windows setup
